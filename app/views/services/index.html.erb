<%= render 'shared/banner' %>

<div class="container">
  
</div>

<div class="container mt-4 mb-3 d-flex align-content-around flex-wrap">
<% @services.each do |service| %>
  <%= render 'shared/card-service', service: service %>
<% end %>
</div>

